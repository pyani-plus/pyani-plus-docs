## Output

By default, no direct output is produced, and all genearated intermediate files are automatically deleted after parsing. The only data retained is stored in the [SQLite3](https://www.sqlite.org/) database, which records all computed values. To retain the intermediate files for further analysis or debugging, specify the ouput directory with the with the `--temp` parameter.