`pyANI-plus` accepts as input a single directory containing FASTA files with the extensions `.fasta`, `.fas`, and `.fna`, and/or their GZip-compressed equivalents like `.fasta.gz`, `.fas.gz`, and `.fna.gz`. Please make sure that your input files match these extensions to ensure that `pyANI-plus` works.