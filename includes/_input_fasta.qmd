`pyANI-plus` accepts as input a single directory containing FASTA files with the extensions `.fasta`, `.fas`, `.fa`, and `.fna`, and/or their GZip-compressed equivalents like `.fasta.gz`, `.fas.gz`, `.fa.gz`, and `.fna.gz`. Please make sure that your input files match these extensions to ensure that `pyANI-plus` works.
